import React, {Component} from 'react';

class Rooms extends Component {

  componentDidMount() {
    window.scrollTo(0,0);
  }

  render() {
    return (
      <div>
        Dev...
      </div>
    );
  }
}

export default Rooms;